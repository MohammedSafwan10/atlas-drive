{
  "name": "atlas-drive",
  "private": true,
  "version": "1.0.0",
  "description": "A cinematic Three.js racing game featuring Alpine Pass, tactical rivals, dynamic weather, and nitro.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler pages deploy dist --project-name=atlas-drive",
    "test": "node --test"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^8.2.2",
    "wrangler": "^4.129.0"
  }
}
